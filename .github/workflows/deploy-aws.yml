name: Deploy AWS

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Implement in OpenSpec change 008-aws-deployment-hardening"
